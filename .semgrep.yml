rules: []

exclude:
  - path: tests/

# Generic secret-detection rules (AWS keys, private keys, API keys, GitHub
# tokens) now live in ssl_pydev's own bundled baseline (scripts/semgrep-rules.yml),
# applied automatically by `ssl-pydev security` / `just security`. This file
# only carries project-specific config (excludes) layered on top of that
# baseline plus the p/ci registry pack.
